import React, {FC} from 'react';
import Icons from 'react-native-vector-icons/MaterialIcons';

const EditButton: FC = () => {
  return <Icons name="settings" color="gray" size={25} />;
};

export default EditButton;
